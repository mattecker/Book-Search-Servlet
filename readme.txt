Matthew Ecker
Book Search Servlet

This servlet will search the database for an ISBN given by the user, and return information about the book from a database. The database must be named "BookDB" and have a user/pass of root/root. The supplied Book.sql will generate the data.